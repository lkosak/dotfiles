# This file just sets the ZDOTDIR, and then everything else lives in there
# (.zshrc, caches, etc.)
#
# Have to keep this in $HOMEDIR so it gets loaded on shell initialization.
export ZDOTDIR=$HOME/.config/zsh
